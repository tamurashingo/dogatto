# Phase 6 実装レビュー結果

**レビュー日時**: 2026-02-09 06:30  
**レビュー対象**: front/src/pages/LabelsPage.tsx

## 実装状況サマリー

### ✅ 完全実装済み

#### T019: ラベル一覧ページの実装
- [x] ページファイル作成 (`LabelsPage.tsx` - 345行)
- [x] ラベル一覧取得 (Line 37-65: `fetchLabels`)
- [x] 検索機能
  - Line 28-29: 検索モード・クエリの状態管理
  - Line 246-266: 検索フォームUI
  - Line 193-196: 検索ハンドラー
- [x] フィルター機能 (Line 30, 268-274)
- [x] ソート機能 (Line 31-32, 276-286)
- [x] 統計情報表示 (Line 228-242)
- [x] 空状態の実装 (Line 299-310)
- [x] ローディング状態 (Line 296-297)
- [x] エラー表示 (Line 290-294)

#### T021: 検索機能の実装
- [x] 検索モード切り替え (Line 247-254: label_name/tag_name)
- [x] 検索入力フォーム (Line 255-261)
- [x] クリアボタン (Line 263-265, Line 201-203)

#### T022: フィルター・ソート機能の実装
- [x] フィルタードロップダウン (Line 269-274: all/used/unused)
- [x] ソートドロップダウン (Line 276-282)
  - name, tag_count, todo_count, updated_at
- [x] ソート順変更 (Line 284-286: ↑/↓ ボタン)

### ❌ 未実装項目

#### 1. Debounce処理 (P1 - 重要度: 中)
**現状**:
- useDebounceフックは存在 (`/front/src/hooks/useDebounce.ts`)
- しかしLabelsPage.tsxで**使用されていない**
- 検索は手動送信（Searchボタンクリック）のみ

**影響**:
- ユーザーが入力中にリアルタイムで検索されない
- Searchボタンを押す必要がある
- UXとしては許容範囲だが、タスクには「Debounce処理（300ms）」が明記されている

**推奨**: 実装する（推定30分）

#### 2. 検索結果のハイライト (P2 - 重要度: 低)
**現状**: 未実装
**影響**: 検索語が結果内でハイライトされない
**推奨**: Phase 6では保留、Phase 7以降で検討

#### 3. URLクエリパラメータ連携 (P2 - 重要度: 低)
**現状**:
- `useSearchParams`や`URLSearchParams`の使用なし
- フィルター・検索状態がURLに反映されない

**影響**:
- ブラウザの戻る/進むで状態が復元されない
- URLを共有しても同じフィルター状態にならない

**推奨**: Phase 6では保留、必要に応じて後で実装

#### 4. ページネーション (P1? - 重要度: 要確認)
**現状**:
- tasks.mdのT019に「ページネーション」が記載されている
- しかしLabelsPage.tsxには実装されていない
- APIは page/per_page パラメータをサポート

**影響**:
- 全ラベルが一度に取得・表示される
- ラベル数が多い場合にパフォーマンス問題の可能性

**推奨**: ラベル数の想定による
- 想定ラベル数が少ない（<100）→ 不要
- 多い場合（>100）→ 実装推奨（推定1-2時間）

### 🟡 実装済みだが改善の余地

#### useEffect依存配列の不完全性
**Line 213-215**:
```tsx
useEffect(() => {
  fetchLabels();
}, [filter, sortBy, sortOrder]);
```

**問題**:
- `searchQuery`と`searchMode`が依存配列に含まれていない
- これらが変更されても自動的にfetchされない
- 現状はSearchボタンで手動送信するため問題なし

**推奨**: Debounce実装時に修正

## T020: ラベル詳細ページ（P2 - 保留）

**現状**: 未実装  
**代替実装**: ラベルクリックで`/todos?label=${ulid}`に遷移（Line 185）  
**判断**: P2タスクのため、現時点では不要

## Phase 6 完了判定

### T019: ラベル一覧ページ
- **ステータス**: ✅ **完了** (必須機能はすべて実装済み)
- **備考**: Debounce未実装だが、手動検索で機能的には完結

### T020: ラベル詳細ページ
- **ステータス**: ⏸️ **保留** (P2タスク)

### T021: 検索機能
- **ステータス**: ✅ **完了** (Debounceなしで機能する)
- **備考**: Debounce実装は改善項目

### T022: フィルター・ソート機能
- **ステータス**: ✅ **完了**
- **備考**: URLクエリパラメータ未実装だが必須ではない

## 推奨アクション

### オプション1: 現状のまま完了とする ⭐ (推奨)
**理由**:
- 全必須機能は実装済み
- Debounceは「あると良い」機能（Nice-to-have）
- ページネーションは現時点で不要の可能性が高い
- URLクエリパラメータはP2レベル

**アクション**:
1. tasks.mdを更新し、Phase 6を完了とマーク
2. 改善項目（Debounce, URLパラメータ）をPhase 12（改善フェーズ）に追加

**推定時間**: 15分

### オプション2: Debounceを実装してから完了とする
**理由**:
- tasks.mdに「Debounce処理（300ms）」が明記されている
- UX向上

**アクション**:
1. useDebounceをLabelsPage.tsxに統合
2. searchQueryをdebounce
3. useEffect依存配列を修正
4. テスト
5. tasks.mdを更新

**推定時間**: 45分

### オプション3: Debounce + ページネーションを実装
**理由**:
- タスク定義に忠実
- 将来のスケール対応

**アクション**:
1. Debounce実装
2. ページネーション実装
3. テスト
4. tasks.mdを更新

**推定時間**: 2-3時間

## 提案

**オプション1を推奨**します：

**理由**:
1. Phase 6の本質（ページの実装）は完了している
2. Debounceはあると良いが、現状でもユーザーは不便を感じない
3. 早くPhase 7-11を完了させ、全体像を把握する方が重要
4. 改善項目は後で優先度を判断して実装できる

**判断基準**:
- プロダクトの完成度を優先 → オプション1
- タスク定義の厳密な遵守を優先 → オプション2
- 将来のスケール対応を優先 → オプション3

---

**次のステップ**: ユーザー（あなた）の判断を待つ
- オプション1 → tasks.md更新、Phase 7へ
- オプション2 → Debounce実装
- オプション3 → Debounce + ページネーション実装
